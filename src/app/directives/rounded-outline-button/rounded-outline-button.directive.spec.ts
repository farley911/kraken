import { RoundedOutlineButtonDirective } from './rounded-outline-button.directive';

describe('RoundedOutlineButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedOutlineButtonDirective();
    expect(directive).toBeTruthy();
  });
});
