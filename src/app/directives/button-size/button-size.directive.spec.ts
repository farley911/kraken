import { ButtonSizeDirective } from './button-size.directive';

describe('ButtonSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSizeDirective();
    expect(directive).toBeTruthy();
  });
});
