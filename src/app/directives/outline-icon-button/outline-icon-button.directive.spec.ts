import { OutlineIconButtonDirective } from './outline-icon-button.directive';

describe('OutlineIconButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineIconButtonDirective();
    expect(directive).toBeTruthy();
  });
});
